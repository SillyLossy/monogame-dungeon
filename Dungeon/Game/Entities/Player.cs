using Microsoft.Xna.Framework;

namespace Dungeon.Game.Entities
{
    public class Player : MovableEntity
    {
        public Player(Point initialPosition) : base(initialPosition)
        {
        }
    }
}
